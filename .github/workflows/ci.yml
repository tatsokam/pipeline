name: Build Workflow

on:
  push:guy
    branches:
      - main

jobs:
  Test:
    runs-on: ubuntu-latest
    container:
      image: alpine
    steps:
      - name: Run un test
        run: echo "Franck"